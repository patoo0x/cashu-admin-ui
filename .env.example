# Cashu Nutshell Admin UI — Configuration
# Copy this file to .env and modify as needed.
#
# Variable names follow Nutshell's naming convention where possible.
# See: https://github.com/cashubtc/nutshell (.env.example)

# --------- ADMIN UI SERVER ---------

# Port for the admin dashboard
PORT=3339

# Auth type: 'basic' (HTTP Basic Auth), 'token', or 'none'
# WARNING: Set AUTH_TYPE=none only for local development.
AUTH_TYPE=basic

# Credentials for HTTP Basic Auth (change in production!)
ADMIN_USER=admin
ADMIN_PASS=admin123

# --------- NUTSHELL MINT CONNECTION ---------

# URL of the Nutshell mint's /v1 REST API.
# Default: localhost:3338 (Nutshell's default MINT_LISTEN_PORT)
MINT_URL=http://127.0.0.1:3338

# gRPC Management RPC port (reserved for future direct gRPC integration).
# Must match Nutshell's MINT_RPC_SERVER_PORT setting.
# Default: 8086 (same as Nutshell)
MINT_GRPC_PORT=8086

# --------- DATABASE INSPECTION ---------

# Path to the Nutshell mint's SQLite database file.
# Used for direct database entry counts (mint quotes, melt quotes, proofs).
# This is the MINT_DATA_PATH/cashu.db from Nutshell's settings.
#
# Common paths:
#   Default install:  ~/.cashu/mint/data/cashu.db
#   Docker (compose): ./data/cashu.db  (mounted from nutshell container)
#   Custom:           set to match MINT_DATA_PATH in your nutshell .env
#
# Set to empty string to disable DB inspection (counts will show N/A).
# Read-only access is sufficient — the admin UI never writes to the DB.
MINT_DB_PATH=

# --------- PROMETHEUS ---------

# Metrics are always available at GET /metrics (no auth).
# Add to your prometheus.yml:
#   scrape_configs:
#     - job_name: 'cashu-admin'
#       static_configs:
#         - targets: ['localhost:3339']
